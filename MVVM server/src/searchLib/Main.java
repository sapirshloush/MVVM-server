package searchLib;

import java.util.ArrayList;



////public class Main {
////
////	public static void main(String[] args) {
////		Searcher<Position> searcher=new BFS<Position>();
////
////		@SuppressWarnings("unchecked")
////		Searchable<Position> searchable= (Searchable<Position>) new MySearchable<Position>();
////		ArrayList<Action<Position>> actions = searcher.search(searchable);
////
////		for(Action<Position> a : actions)
////	    System.out.println(a);
////		System.out.println("# of nodes evaluated " + searcher.getNumberOfNodesEvaluated());
////
////	}
//
//}
